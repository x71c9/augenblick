[package]
name = "augenblick"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
x11rb = { version = "0.13", features = ["allow-unsafe-code"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
